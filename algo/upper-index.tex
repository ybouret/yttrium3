\documentclass[aps,12pt]{revtex4}
\usepackage[a4paper]{geometry}
\usepackage{graphicx}
\usepackage{amssymb,amsfonts,amsmath,amsthm}
\usepackage{bm}
\usepackage{pslatex}

\begin{document}

We have a $n \times n$ matrix, and we want to index the upper diagonal with $$i \in [1:n],\; j \in [i:n]$$
leading to $\dfrac{n(n+1)}{2}$ terms.

The last index for each $i$ is:
\begin{equation}
K_i = \sum_{l=1}^i (n+1-i) = i(n+1) - i(i+1)/2 
\end{equation}

For a given $k$, $i$ is the greater than or equal to integer matching:
$$
	i^2 - (1+2n) i + 2 k = 0
$$

$$
	i = \dfrac{ \left[ (1+2n) - \sqrt{ (1+2n)^2 - 8k }\right]_{\bar 2}}{2}
$$

$$
	j = (i-1) + k - K_{i-1}
$$


\end{document}
